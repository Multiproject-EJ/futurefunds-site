# FutureFunds environment template
# Copy this file to `.env` and fill in the values before running any Supabase functions.

# ---- Supabase project ----
# Base URL for your Supabase project (https://PROJECT_REF.supabase.co)
SUPABASE_URL=https://YOUR-PROJECT.supabase.co
# Public anon key used by the web client
SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY
# Service role key used by edge functions for privileged queries
SUPABASE_SERVICE_ROLE_KEY=YOUR_SUPABASE_SERVICE_ROLE_KEY
# Optional: Database connection string for local migration runners
SUPABASE_DB_URL=postgresql://postgres:postgres@127.0.0.1:5432/postgres
# Optional: project ref used when deploying functions with the Supabase CLI
SUPABASE_PROJECT_REF=YOURPROJECTREF

# Automation secret that unattended jobs must send via x-automation-secret header
AUTOMATION_SERVICE_SECRET=choose-a-long-random-string
# Per-user run quota enforced by runs-create (defaults to 5 if unset)
RUNS_DAILY_LIMIT=10

# ---- Model credentials ----
# Primary OpenAI API key (used as fallback when a credential record is missing)
OPENAI_API_KEY=sk-your-openai-key
# Optional override for the OpenAI base URL (defaults to https://api.openai.com/v1)
OPENAI_API_BASE=https://api.openai.com/v1
# Optional OpenAI organisation id if your account requires it
OPENAI_ORG_ID=
# Optional OpenRouter API key for models hosted there
OPENROUTER_API_KEY=

# ---- Retrieval ingest controls ----
# Milliseconds to wait between embedding requests in the docs-process worker
DOC_EMBED_DELAY_MS=120

# ---- Cache controls ----
# Default TTL (minutes) for cached LLM completions reused across runs
AI_CACHE_TTL_MINUTES=10080
# Optional stage overrides
STAGE1_CACHE_TTL_MINUTES=
STAGE2_CACHE_TTL_MINUTES=
STAGE3_CACHE_TTL_MINUTES=
FOCUS_CACHE_TTL_MINUTES=

# ---- Ticker ingestion (optional) ----
# Default JSON/CSV endpoint the tickers-refresh worker will fetch when no records array is provided
TICKER_FEED_URL=
# Optional API key sent as Authorization: Bearer <key> when fetching the ticker feed
TICKER_FEED_API_KEY=

